%script
  = "display_map(#{@location.latitude}, #{@location.longitude}, 13);"
%script
  #markers
    - @venues.each do |v|
      %script
        = "add_marker(#{v.latitude}, #{v.longitude}, 'Title', '#{link_to("#{v.name}", home_path)}');"